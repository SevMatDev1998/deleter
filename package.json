{
  "name": "itbuild.app.landing",
  "version": "1.0.0",
  "main": "dist/index.html",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx live-server dist",
    "serve": "live-server --port=3000 dist",
    "locale:watch": "node scripts/watch.js",
    "dev": "concurrently \"npm run locale:watch\" \"npm run serve\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "concurrently": "^9.2.0",
    "esbuild": "^0.25.5",
    "fs-extra": "^11.3.0",
    "husky": "^9.1.7",
    "live-server": "^1.2.2",
    "sass": "^1.89.2"
  }
}
